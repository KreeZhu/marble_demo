# 《弹珠》游戏设计文档

**文档版本：** 1.0  
**对应游戏版本：** v0.1.0 当前开发版  
**规则基线日期：** 2026-08-13<br>
**文档用途：** 统一策划、开发、美术、音效和测试人员对当前游戏的理解，并作为后续逐条检查规则的基准。
**在线编辑版：** [Google 文档](https://docs.google.com/document/d/1XyUlNziW33uy8o0GoU3LKci7qbUbTkiJHrhe92RRCog/edit?usp=drivesdk)

---

## 1. 项目概述

### 1.1 一句话说明

《弹珠》是一款固定力度、自由调角度的二维物理解谜游戏。玩家需要预判有限距离内的弹道，利用不同形状和材质的墙、传送门、移动机关、开关门及可接球的发射器，让同一颗球最终碰到 B 点。

### 1.2 核心目标

游戏不是比拼快速反应或连续操作，而是让玩家完成以下思考过程：

1. 阅读箱庭布局和不同颜色组件的含义。
2. 预测球的初段路线、碰撞位置和反射方向。
3. 选择是否借助墙、传送门、开关门或空发射器改变路线。
4. 用有限长度的预线调整角度。
5. 发射并观察实际结果。
6. 根据失败轨迹修正下一次尝试。

### 1.3 设计支柱

**规则简单，组合复杂。** 玩家主要只调整方向并发射，复杂度来自关卡元素之间的组合，而不是来自大量操作按键。

**所有摆放都应有意义。** 关卡内的元素应承担关键路线、限制路线、提供替代路线或误导玩家的作用，不能只是无意义装饰。

**允许玩家发现非标准解法。** 关卡有推荐解法用于验证设计，但推荐解法不是通关条件。任何路线只要让球碰到 B 点都应算胜利。

**限制外框滥用。** 大多数关卡的四周边界会卸掉球的速度，避免玩家反复借外框随机碰撞通关。只有明确教学或需要外框反弹的关卡才开放指定边界。

**后期强调单球连续解谜。** 开关状态只能由同一颗仍在本次尝试中的球继续利用。球可以被发射器接住后再次发射，但一旦失败，机关会立即复位并准备下一次尝试。

### 1.4 目标玩家

- 喜欢轻量物理解谜、弹射和路径规划的玩家。
- 不要求掌握复杂键位或高强度操作。
- 能通过颜色、形状、动画和简短提示理解规则。
- 适合桌面浏览器游玩；指针操作也兼容触摸输入，但当前界面以电脑横屏体验为主。

### 1.5 当前平台与内容规模

- 平台：浏览器。
- 画面：二维 Canvas 箱庭。
- 官方关卡：25 关。
- 关卡菜单：横向每行 5 关，所有关卡均可直接选择，不设解锁门槛。
- 自定义内容：内置关卡编辑器，可新建、编辑、试玩并保存关卡。
- 发射力度：所有发射器固定为 700，玩家不能调整。

---

## 2. 玩家流程

### 2.1 主流程

1. 玩家进入游戏开始界面。
2. 选择“开始游戏”进入关卡菜单，或选择“退出游戏”。
3. 在关卡菜单中任选一个官方关卡或已保存的自定义关卡。
4. 进入关卡后，观察布局、当前有球发射器和瞄准预线。
5. 调整方向并发射。
6. 球在箱庭中移动、反弹、触发机关、传送或进入发射器。
7. 球碰到 B 点时立即通关。
8. 通关弹窗左侧为“返回关卡菜单”，右侧绿色高亮按钮为“下一关”。
9. 最后一关通关时不再显示下一关入口，只返回关卡菜单。

### 2.2 单次关卡循环

**观察 → 瞄准 → 发射 → 读取结果 → 修正。**

- 发射成功命中 B：结束关卡。
- 球进入任意发射器：本次尝试继续，玩家获得一次新的手动瞄准与发射机会。
- 球停下、碰到卸力区域或离开有效行动：本次尝试失败。
- 失败判定成立：立即从 A1 准备新尝试，开关、门和移动机关同步复位。
- 玩家主动按“重置”：立即完整重置当前关卡。

### 2.3 胜利后的流程

- 不提供“重玩当前关”按钮。
- “返回关卡菜单”为深色普通按钮。
- “下一关”为绿色高亮主按钮。
- 关卡菜单会在当前会话中标记已完成关卡。
- 当前完成记录不会跨浏览器刷新永久保存。

---

## 3. 核心规则与判定优先级

### 3.1 唯一胜利条件

**球的运动路径只要接触 B 点，就立即通关。**

以下内容都不是强制胜利条件：

- 是否碰过指定墙。
- 是否使用传送门。
- 是否击中过开关。
- 是否进入过 R 发射器。
- 是否使用过所有发射器。
- 是否采用关卡作者的推荐路线。

关卡数据中的“推荐机制”只用于检查关卡作者设计的路线是否成立，不用于阻止玩家通关。

### 3.2 同一帧判定优先级

为避免边界情况产生争议，当前优先级如下：

1. 球的本帧运动路径是否接触 B 点。
2. 墙、卸力区域等碰撞结果。
3. 开关触发与红门打开。
4. 传送门传送。
5. 是否进入发射器并被接住。
6. 是否因速度过低而停止。

因此：

- 球在同一帧既经过 B 点又撞上卸力墙，按命中 B 点通关。
- 球在同一帧既经过 B 点又进入发射器，按命中 B 点通关。
- 球高速穿过 B 点而没有在 B 点中心停留，也算命中。
- 传送造成的位置跳跃不会把“入口到出口之间的虚拟直线”误判为经过 B 点。

### 3.3 一次尝试的定义

一次尝试从 A1 发射开始，直到发生以下任一情况：

- 球命中 B 点并通关。
- 球停下。
- 球碰到卸力墙或卸力边界。
- 玩家主动重置关卡。

球进入 A 或 R 发射器后只是暂时停止等待玩家操作，**不会结束本次尝试**。

### 3.4 失败后的复位

失败判定成立后，系统立即统一复位，不再等待玩家按下下一次发射：

- 所有红色按钮恢复 OFF。
- 所有红门恢复关闭。
- 所有移动机关恢复初始位置、相位和运动状态。
- 本次尝试累计的机关使用记录清空。
- 球重新由 A1 持有。
- 上一发的失败轨迹不随复位清除，仍按原有时长短暂显示。

主动按“重置”时，上述复位立即发生，并同时恢复关卡预设的发射器方向。

---

## 4. 发射器系统

### 4.1 统一功能原则

所有发射器在接到球以后，功能完全相同：

- 接住球并让球停止。
- 保持当前尝试中的开关和门状态。
- 允许玩家重新调整方向。
- 显示当前发射器的有限长度瞄准预线。
- 由玩家主动按下发射键进行下一次发射。
- 发射力度固定为 700。

**没有任何发射器会在接球后自动发射。**

### 4.2 A 与 R 的区别

A 和 R 是关卡制作阶段的两类组件，不是两套不同的发射功能。

| 类型 | 开局状态 | 关卡中的用途 | 接到球后 |
|---|---|---|---|
| A 发射器 | A1 开局有球 | 定义关卡的初始出发点 | 可重新瞄准并手动发射 |
| R 发射器 | 开局为空 | 定义中途接球和改变路线的节点 | 与 A 完全相同，可重新瞄准并手动发射 |

新建关卡只能有一个开局持球的 A 发射器。R 发射器数量不限。

### 4.3 视觉状态

- 绿色发光：该发射器当前有球，可以瞄准和发射。
- 紫色发光：该发射器当前为空，不能发射。
- 发射器按钮文字显示“有球”或“空”。
- 球飞行时，所有发射器都暂时没有发射权。
- 球进入新的发射器后，旧发射器变空，新发射器变绿并获得控制权。

### 4.4 瞄准权限

- 只有当前有球的发射器可以调整方向。
- 空发射器不能提前调整方向。
- 空发射器不显示玩家瞄准预线。
- 球进入空发射器后，玩家才看到该发射器的预线并开始第二段瞄准。
- 编辑器是设计工具，为方便设计者校验路线，会显示 A 与 R 的设计预线；这不代表玩家游玩时能提前看到空发射器预线。

### 4.5 位置与力度

- 游玩过程中，A 与 R 发射器都不能移动位置。
- 玩家只能调整当前有球发射器的方向。
- 所有发射器默认方向为向右，即 0 度。
- 所有发射器力度固定为 700。
- 玩家界面不显示可调力度选项。
- 编辑器可以摆放 A 和 R 的位置，但保存进入游玩后位置固定。

### 4.6 发射器接球状态机

```text
开局：A1 有球，所有 R 为空
        |
        v
玩家调整 A1 方向并发射
        |
        v
球飞行中，所有发射器无控制权
        |
        +-- 碰到 B ----------> 通关
        |
        +-- 进入 A 或 R -----> 该发射器有球
        |                         |
        |                         v
        |                    玩家重新瞄准
        |                         |
        |                         v
        |                    手动二次发射
        |
        +-- 停止或被吸住 ----> 本次尝试失败
                                  |
                                  v
                         立即复位机关，A1 重新持球
```

### 4.7 发射计数

- 游戏不显示发射次数。
- 游戏不累计或保存发射次数。
- 通关结果不使用发射次数进行评分或评价。

---

## 5. 弹球、瞄准与轨迹反馈

### 5.1 弹球

- 每关始终围绕同一颗球进行。
- 球有位置、速度、半径和运动轨迹状态。
- 飞行过程中速度会有轻微衰减，避免无限运动。
- 当速度低于停止阈值时，本次尝试判定失败。

### 5.2 瞄准预线

- 形式：点状虚线。
- 作用：预览球前段的预计路线、有限次数的碰撞和传送结果。
- 默认长度：480。
- 玩家可调范围：260 至 760，每次调整 20。
- 预线不是完整答案，不会显示整条无限路线。
- 预线使用当前实际机关状态；同一颗球开门后进入发射器，再次瞄准时，预线会按已经打开的门计算。
- 空发射器没有预线。
- 当前瞄准预线不会因为镜头尺寸无限延伸。

### 5.3 上一次瞄准参考

- 发射后，发射前的虚线会作为参考继续显示。
- 在下一次发射前，玩家可用它比较预想路线与实际路线。
- 下一次发射时，它会被新的瞄准记录替换。

### 5.4 失败轨迹

- 实际飞行轨迹使用细实线，不与瞄准虚线混淆。
- 失败后会短暂保留完整轨迹并逐渐淡出。
- 当前保留时间约 4.2 秒。
- 下一次发射会清除旧失败轨迹。
- 该轨迹用于帮助玩家判断误差，而不是提供自动修正。

---

## 6. 墙体与碰撞系统

### 6.1 墙体形状

所有墙体都属于同一个“墙”组件体系，通过形状和材质产生差异。

| 形状 | 主要作用 | 设计特征 |
|---|---|---|
| 矩形墙 | 阻挡、走廊、平面反射 | 可调宽、高和旋转角度 |
| 三角墙 | 斜面反射、改变大方向 | 可调宽、高和旋转角度 |
| 圆形墙 | 曲面反射、细分入射角 | 可调半径；旋转不改变圆形物理结果 |

### 6.2 墙体材质

墙体形状与材质相互独立。矩形、三角形和圆形都可选择以下材质：

| 材质 | 颜色 | 碰撞结果 | 设计用途 |
|---|---|---|---|
| 普通墙 | 金属灰 | 正常反弹并略有能量损失 | 主要解谜反射面、阻挡和走廊结构 |
| 弹力墙 | 绿色 | 反弹并提高速度 | 延长路线、强化转向、制造高速段 |
| 卸力墙 | 黄色 | 速度归零，本次尝试失败 | 禁止区域、封死捷径、惩罚错误路线 |

### 6.3 旋转与尺寸

- 编辑器中所有墙都可设置角度。
- 矩形墙和三角墙可通过可视化手柄直接旋转。
- 矩形墙和三角墙可通过尺寸手柄调整宽高。
- 数字属性面板也可精确输入位置、宽、高、半径和角度。
- 旋转以墙体中心为轴。

### 6.4 外框边界

每个关卡的上、下、左、右四条边界可分别设为：

- 普通反弹边界。
- 黄色卸力边界。

官方关卡设计原则：

- 不需要外框反弹时，尽量把四边设为卸力。
- 需要外框教学时，只开放必要的一边。
- 外框反弹必须具有明确设计意义，不能成为万能随机解法。

---

## 7. 移动机关

### 7.1 基础规则

- 移动机关本质上是带往复路径的墙体。
- 可设置水平移动距离、垂直移动距离和速度。
- 运动采用平滑周期往复方式。
- 可以使用普通、弹力或卸力材质；官方计时机关通常使用卸力材质。

### 7.2 设计作用

- 制造可通行与不可通行的时间窗口。
- 要求玩家不仅判断角度，还判断发射时机。
- 与开关门或发射器接球组合时，形成“先准备状态，再等待窗口”的多阶段谜题。

### 7.3 复位规则

- 同一颗球被发射器接住时，移动机关继续本次尝试的状态。
- 球失败后，移动机关立即回到关卡初始状态。
- 玩家主动重置时立即回到初始状态。

---

## 8. 传送门

### 8.1 基础规则

- 传送门成对存在，目前以蓝色和橙色区分。
- 球进入任意一端，会从配对端出现。
- 传送前后的运动速度方向保持连续。
- 出口方向参数用于确定球在出口圆周外的出现位置，避免立即再次触发同一传送门。
- 传送后有短暂冷却时间，防止球在一对门之间无限瞬间来回。

### 8.2 设计作用

- 跨越无法通过的黄色隔断。
- 将关卡分成两个空间区域。
- 保留入射方向，让玩家需要提前计算出口后的路线。
- 与圆形墙、弹力墙、移动机关和红门组合。

### 8.3 限制

- 传送门不会自动帮玩家转向。
- 传送门本身不是通关条件；如果玩家找到其他路线碰到 B，一样通关。
- 传送形成的位置跳跃不会被当作一条真实飞行直线进行 B 点碰撞检测。

---

## 9. 红色开关与红门

### 9.1 红色按钮

- 初始状态显示 OFF。
- 使用红色灯光和呼吸动画提示可互动。
- 球接触后切换为 ON。
- 触发时播放清晰的电子开关音效。

### 9.2 红门

- 初始状态关闭并显示 OFF。
- 与红色按钮通过红色视觉连线建立关系。
- 任意红色按钮被触发后，同色红门打开并显示 ON。
- 打开后的门不再参与碰撞，球可以穿过。

### 9.3 状态保持

- 球触发按钮后继续飞行：门保持打开。
- 球进入 A 或 R 发射器：门保持打开，允许同一颗球重新瞄准后继续。
- 球失败：本次尝试结束。
- 失败判定成立：按钮立即恢复 OFF，红门立即重新关闭，A1 重新持球。
- 主动重置或重新进入关卡：按钮和门立即恢复初始状态。

### 9.4 当前颜色范围

当前版本只实现红色按钮与红门。编辑器中也只提供这一组颜色。未来若增加多色电路，应让每种按钮只控制同色门。

---

## 10. 音效与触觉反馈目标

### 10.1 发射音效

- 清楚、有力度，不能被其他碰撞声掩盖。
- 包含机械释放、低频冲击和类似物体高速穿过空气的声尾。
- 带适量回声，符合霓虹科技箱庭的空间感。

### 10.2 事件音效

| 事件 | 声音目标 |
|---|---|
| 普通墙碰撞 | 短促金属碰撞，确认反射发生 |
| 弹力墙碰撞 | 更明亮、上升、能量增强的声音 |
| 卸力墙碰撞 | 低沉、收束、被吸住的声音 |
| 传送 | 上升与空间位移感 |
| 发射器接球 | 清晰的接入与充能提示 |
| 红色按钮 | 开关触发和电路接通感 |
| 通关 | 有辨识度的多音阶完成提示，明显高于普通反馈 |

### 10.3 视觉反馈

- 发射：球体拖尾和保留的瞄准参考。
- 碰撞：通过材质颜色和声音区分结果。
- 接球：发射器产生短暂能量脉冲并切换为绿色。
- 开关：OFF/ON 文本、呼吸光和门状态同步改变。
- 通关：B 点扩散环、粒子和通关弹窗。

---

## 11. 美术与界面方向

### 11.1 总体风格

- 黑色和深色金属为基础。
- 霓虹绿、蓝、紫、红、黄作为功能色。
- 参考未来科技、80 年代电玩厅和三维弹球机的视觉语言。
- 颜色优先表达规则，装饰不能影响组件辨认。

### 11.2 开始界面

- 背景为暗色霓虹弹球封面图。
- 标题“弹珠”为艺术拼接字，并带弹珠滑过字面的轨道感。
- 标题有缓慢呼吸灯效果。
- 标题下方只有“开始游戏”和“退出游戏”两个主要按钮。

### 11.3 关卡菜单

- 继续使用开始界面的封面作为背景。
- 前景采用液态玻璃或毛玻璃效果。
- 官方关卡按顺序编号，每行 5 个正方形入口。
- 已完成、当前、自定义和已编辑关卡有不同边框或状态色。
- 玩家可以自由选择任意关卡。

### 11.4 游玩界面

- 左侧为主要箱庭画布，右侧为紧凑控制面板。
- 控制面板显示关卡编号、名称、考验方向和当前发射器。
- 发射器状态按钮直接显示有球或空。
- 发射、重置、返回菜单和编辑当前关卡均有明确入口。

---

## 12. 操作方式

### 12.1 鼠标或触摸

- 拖动画布：调整当前有球发射器方向。
- 方向滑杆：精确调整 -180 至 180 度。
- 预线长度滑杆：调整 260 至 760。
- 发射按钮：发射当前有球发射器。
- 重置按钮：重置关卡。

### 12.2 键盘

- Space：发射。
- Enter：发射。
- R：重置当前关卡。
- Escape：返回关卡菜单；编辑器试玩时返回编辑器；编辑器中触发退出流程。
- Ctrl+Z / Cmd+Z：编辑器撤销。
- Ctrl+Y / Cmd+Y：编辑器重做。
- Ctrl+Shift+Z / Cmd+Shift+Z：编辑器重做。
- Delete：编辑器删除当前组件。

### 12.3 控制限制

- 球飞行时不能修改方向或再次发射。
- 空发射器不能被选择或提前调整。
- 力度始终固定，所有力度输入均不可用。

---

## 13. 关卡结构与教学顺序

以下“推荐机制”表示作者设计和自动测试所验证的路线，不是强制通关条件。

| 关卡 | 名称 | 主要教学或挑战 | 推荐路线意义 |
|---:|---|---|---|
| 1 | 直线起步 | 基础方向校准 | 保持向右，理解固定力度、发射和 B 点 |
| 2 | 错位窄门 | 斜线精确瞄准 | 用一条斜线穿过两组黄色门缝 |
| 3 | 顶板回廊 | 内部平面反弹 | 避开直线陷阱，借上方普通墙折向 B |
| 4 | 圆柱转角 | 曲面反射 | 击中圆形墙特定位置改变方向 |
| 5 | 绿色弹射 | 弹力材质 | 借绿色三角墙加速并转向 |
| 6 | 跨墙传送 | 传送方向延续 | 用传送门跨过整面黄色隔断 |
| 7 | 传送折角 | 传送加曲面 | 传送后再通过圆形墙完成第二次转向 |
| 8 | 中继换向 | 两段独立瞄准 | A1 射入 R1，R1 接球后由玩家重新瞄准射向 B |
| 9 | 计时回旋 | R、传送、弹力墙与时机 | 多段路线叠加，并避开移动卸力机关 |
| 10 | 红门通电 | 开关门基础 | 同一发先触发按钮，再穿过打开的门 |
| 11 | 反弹开门 | 外框反弹触发 | 只开放上边界，通过外框反弹触发按钮 |
| 12 | 红门节拍 | 回到 A 与状态保持 | 第一发开门并回到 A，第二发等待移动窗口 |
| 13 | 开门跃迁 | 开关接传送 | 同一发完成开锁、穿门、进入传送门 |
| 14 | 双段解锁 | 回收、弹力墙与 R | 先开门并回收，再射入 R 完成后段 |
| 15 | 回环终局 | 多系统连续尝试 | 开门、回到 A、进入 R、等待窗口并传送 |
| 16 | 红门窄廊 | 窄通道开锁 | 用严格水平路线完成按钮、门和 B |
| 17 | 斜线通行证 | 斜线连续开锁 | 在黄色门缝限制下沿同一斜线开门通关 |
| 18 | 曲面钥匙 | 开锁接曲面 | 先开门，再利用圆柱下缘折向 B |
| 19 | 跃迁认证 | 开锁传送精度 | 开门只是前置步骤，随后必须准确进入传送门 |
| 20 | 节拍回收 | 单球状态接力 | 用 A 接回同一颗球，保持红门状态再发射 |
| 21 | 中继通电 | 开锁接 R 换向 | A1 先开锁并射入 R，R 接球后重新瞄准穿门 |
| 22 | 双段伏线 | 回收路线与 R 选择 | 识别错误捷径，完成开门、回收和 R 后段 |
| 23 | 折角密钥 | 开锁、传送、曲面组合 | 同一颗球连续完成三类空间变化 |
| 24 | 计时电路 | 开锁、R、传送与移动窗口 | 同时考验状态准备、二段瞄准和发射时机 |
| 25 | 霓虹回环 | 完整单球连锁 | 综合开门、回收、R、移动机关和传送门 |

---

## 14. 关卡编辑器

### 14.1 编辑器目标

让非开发人员无需修改代码即可制作、命名、试玩、保存和再次编辑可玩的关卡。

### 14.2 地图尺寸

- 小型地图：960 × 600 画布，官方关卡默认尺寸。
- 中型地图：1440 × 900 画布，面积和布局空间显著增大。
- 可一键切换地图尺寸。
- 切换时现有组件按比例缩放和重新定位。

### 14.3 可添加组件

- A 初始有球发射器：每个新关卡只能有一个。
- R 初始为空发射器：可添加多个。
- B 目标。
- 矩形墙。
- 三角墙。
- 圆形墙。
- 红色按钮。
- 红门。
- 移动机关。
- 成对传送门。

### 14.4 可编辑属性

- 通用：X、Y 位置。
- 矩形/三角墙：宽、高、角度、材质。
- 圆形墙：半径、材质；角度可记录但不改变圆形碰撞结果。
- A/R 发射器：位置和方向；力度固定为 700。
- 传送门：位置、半径和出口方向。
- 移动机关：水平移动、垂直移动、速度、形状尺寸、角度和材质。

### 14.5 可视化编辑

- 直接拖动组件改变位置。
- 墙体有类似 Blender 的旋转手柄。
- 矩形和三角墙有可视化尺寸手柄。
- 属性面板可精确输入数值。
- 编辑器会显示所有发射器的设计预线，帮助设计者校验后续路线。

### 14.6 历史操作

- 撤销和重做按钮。
- 支持 Ctrl/Cmd+Z、Ctrl/Cmd+Y 和 Ctrl/Cmd+Shift+Z。
- 最多保留 80 个有效历史快照。
- 连续拖动以一次有效调整记入历史，而不是每个像素都记一步。

### 14.7 试玩流程

1. 设计者选择“试玩当前草稿”。
2. 游戏进入正常物理运行，但标记为编辑器试玩。
3. 试玩中可随时“回到编辑器”。
4. 通关后可选择回编辑器继续修改，或保存并返回关卡菜单。
5. 试玩本身不会自动把草稿当作已完成关卡保存。

### 14.8 保存与退出

- 关卡名称最多 18 个字符。
- 保存要求至少有一个 A 发射器和一个 B 目标。
- 新关卡保存后追加到官方 25 关之后。
- 编辑已有自定义关卡时，保存会替换原版本。
- 官方关卡也可进入编辑器，保存后以本地覆盖版本替换该关显示。
- 有改动时直接退出，会弹出确认：
  - 绿色按钮：保存并退出。
  - 红色按钮：不保存并退出。
- 没有改动时，退出会直接返回菜单。

### 14.9 数据保存范围

- 自定义关卡和官方关卡覆盖数据保存在当前浏览器的本地存储中。
- 当前没有云端同步、账号系统、导出文件或在线分享功能。
- 清除浏览器站点数据会删除这些本地内容。

---

## 15. 当前实现限制与非目标

- 当前只有一颗球，不支持多球同时运行。
- 当前新建关卡只有一个开局持球 A 发射器。
- R 发射器在游玩中不能移动位置。
- 当前只有红色按钮和红门。
- 当前没有可调发射力度。
- 当前没有完整无限轨迹预览。
- 当前没有关卡锁、星级、评分、发射次数统计或永久通关进度。
- 当前没有敌人、生命值、倒计时、随机道具或战斗系统。
- 当前没有在线排行榜、关卡发布平台或多人协作编辑。
- “退出游戏”在浏览器中只能尝试关闭页面；浏览器安全策略可能阻止自动关闭。

---

## 16. 测试与验收标准

### 16.1 核心规则验收

- [ ] 球碰到 B 点必定胜利，不检查其他机关使用情况。
- [ ] 高速球跨过 B 点也能被检测。
- [ ] 传送入口与出口之间的虚拟直线不会误判 B 点。
- [ ] 球进入任意 A 或 R 后停止，不自动发射。
- [ ] 接到球的发射器变绿，并获得瞄准与发射权限。
- [ ] 空发射器保持紫色，不能提前瞄准或发射。
- [ ] 接球后，已打开的门保持打开。
- [ ] 球失败判定成立后立即复位全部机关，并让 A1 重新持球。
- [ ] 失败立即复位后，上一发的细实线轨迹仍按原有规则保留。
- [ ] 主动重置立即复位关卡。
- [ ] 所有发射器力度均为 700。

### 16.2 关卡验收

- [ ] 25 个官方关卡编号连续。
- [ ] 每关至少有一条经过物理模拟验证的推荐解法。
- [ ] 推荐路线中标记为关键的墙确实被使用。
- [ ] 推荐路线不会意外碰到作为陷阱的卸力物体。
- [ ] 移动机关既有可通过窗口，也有会阻挡的时刻。
- [ ] 不需要外框反弹的关卡，推荐路线不依赖外框。
- [ ] R 发射器只在中后期关卡出现。

### 16.3 编辑器验收

- [ ] 可新建、命名、试玩和保存关卡。
- [ ] 可重新编辑官方和自定义关卡。
- [ ] 未保存退出提示正确区分保存和放弃。
- [ ] 试玩后可回编辑器继续修改。
- [ ] 撤销、重做、拖动、旋转和缩放正确。
- [ ] 新关卡不能添加第二个开局有球 A 发射器。
- [ ] R 在编辑器中标记为初始为空。

---

## 17. 需要共同确认的规则清单

以下条目是当前已经实现的设计理解。后续讨论可直接按编号修改：

1. **胜利条件：** 只要球碰到 B 点就胜利，任何机关都不是强制条件。
2. **发射器统一：** A 与 R 接到球后的功能完全相同，都由玩家重新瞄准并手动发射。
3. **初始差异：** A1 开局有球；R 开局为空。绿色表示有球，紫色表示空。
4. **空载信息：** 空发射器不能提前调整，也不向玩家显示预线。
5. **编辑器例外：** 编辑器可以显示空发射器设计预线，方便关卡制作。
6. **发射器位置：** 游玩时所有发射器位置固定，只能调整当前有球发射器的方向。
7. **力度：** 所有发射器固定 700，不提供力度设置。
8. **单球连续性：** 球被任何发射器接住都仍属于同一次尝试，机关状态保留。
9. **失败条件：** 球停止或被卸力区域吸住后，本次尝试结束。
10. **复位时点：** 球停止或被卸力区域吸住并确认失败时，机关立即复位且 A1 重新持球；发射器成功接球时不复位。
11. **发射计数：** 游戏不显示、累计、保存或评价发射次数。
12. **推荐解法：** 只用于关卡设计验证，不限制玩家自由解法。
13. **新建关卡：** 只能有一个开局有球的 A 发射器，可有多个初始为空的 R 发射器。
14. **红色电路：** 当前任意红按钮会打开同色红门；同一次尝试内保持开启。
15. **外框原则：** 除非关卡明确需要，否则外框应使用卸力材质，限制随机反弹。

---

## 18. 文档维护规则

- 未经关卡作者明确许可，不得修改任何已设计、已编辑或已保存关卡的名称、布局、坐标、组件和推荐路线；新的关卡创意只能通过新增关卡实现。
- 每次修改核心机制时，先更新第 17 节的确认条目。
- 实现完成后，再同步更新对应的详细章节和验收清单。
- 新增关卡元素时，必须写清楚视觉识别、物理结果、状态保持、复位条件和编辑器属性。
- 关卡推荐机制可以变化，但不得重新成为强制胜利条件。
- 文档描述与实际游戏不一致时，以双方最新确认的规则为目标，并安排代码修正，不能让不一致长期存在。
